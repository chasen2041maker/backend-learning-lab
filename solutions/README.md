# 参考答案使用规则

参考答案不是第一步。

- SQL 的部分参考查询在 `exercises/sql-postgres/reference-queries.sql`；
- Python/Go 示例本身是分层基线，README 中的扩展任务需要你独立完成；
- 综合项目没有整套最终答案，因为设计和故障验证才是学习重点；
- 卡住 30 分钟后，优先按 `HOW_TO_ASK_GPT.md` 请求一个提示；
- 看过答案后，第二天必须从空白重新实现并通过测试。

提交答案时必须附：需求、数据流、失败窗口、验证命令和仍未覆盖的风险。只有代码 diff 不算完成。
