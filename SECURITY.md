# 公开学习仓库安全规则

本仓库是公开仓库。提交前必须确认：

- 没有公司源代码、内部设计文档、内部域名、IP、账号、Token、密钥或真实日志；
- 没有客户、用户、同事的身份信息或业务数据；
- `.env`、数据库文件、构建产物未被提交；
- 示例公司、用户、证券、工单和事件全部是虚构数据；
- 公开依赖不存在已知高危漏洞，镜像不包含密钥；
- 截图中没有浏览器账号、终端路径外的敏感内容。

如果不确定某段内容能否公开，不要提交。重新写一个能表达同一工程原理的最小原创示例。
