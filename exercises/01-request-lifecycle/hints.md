# 第 1 课三级提示

## 提示 1

先判断 `status_code`。如果连 HTTP 状态码都没有，客户端并未收到 HTTP 响应。

## 提示 2

收到 HTTP 响应后，客户端应优先记录稳定的 `code`，而不是解析可能变化的
`message`。使用字典的 `.get()` 读取 Body 字段。

## 提示 3

两个函数都只需要条件判断和返回值，不需要网络库、异常类或全局变量。不要把
`400`、`401`、`404`、`422` 写成新的中文字符串；保留响应中的稳定错误码。
