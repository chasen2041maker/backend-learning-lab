# 如何让 GPT 教你、提示你和审查整个仓库

GPT 可以提高反馈速度，但不能替代你建立心智模型。不同阶段使用不同模板。

## 学习概念：不要直接给最终答案

```text
我是后端初学者，正在学习：[概念]。
我目前能用自己的话解释：[内容]
我不理解：[具体问题]
它出现在：[文件路径/课程]

请不要直接替我完成最终练习。请按顺序：
1. 先检查我的理解哪里正确、哪里错误；
2. 用一个生活类比解释，并指出类比不准确之处；
3. 给一个不超过 20 行的 Python 或 Go 最小例子；
4. 画出输入、状态变化、输出和失败点；
5. 只给我一道需要修改一处代码的练习；
6. 最后问我两个问题，等我回答后再纠正。
```

## 卡住时：只要一个提示

```text
我的目标：[一个很小的目标]
预期：[结果]
实际：[结果]
完整错误：[不要只贴最后一行]
相关最小代码：[代码]
我已经验证：[事实]
我的根因假设：[假设]

请先不要给完整代码。只告诉我：
1. 我的假设是否能解释现有证据；
2. 下一步最小实验是什么；
3. 如果实验结果分别为 A/B，下一步怎么走。
```

## 读懂 AI 生成的代码

```text
这段代码由 AI 辅助生成，我需要把它变成自己的能力。
文件：[路径]
功能：[目的]
代码：[最小完整范围]

请不要先重构。请逐步说明：
1. 谁调用它、输入从哪里来、输出到哪里；
2. 每个状态在哪里改变；
3. 事务/锁/超时/重试边界；
4. 在哪一行宕机会产生什么结果；
5. 哪些错误被转换、哪些会继续传播；
6. 给我三个故障注入实验；
7. 要求我关闭答案重新讲解，再根据我的回答纠正。
```

## 审查一个小功能

```text
请审查这次后端修改，优先找真实正确性、安全和可靠性问题。

需求：
边界/不做什么：
契约变化：
数据与事务：
并发和失败窗口：
验证命令：
已知限制：
git diff：

请用 P0/P1/P2/P3 标注严重程度。每个问题必须包含：
- 具体文件和位置；
- 可触发的输入/并发/故障场景；
- 为什么现有测试没有挡住；
- 最小修复；
- 修复后的验证方式。
如果没有确定问题，请明确说没有发现，不要凑数量。
```

## 整个仓库送审模板

完成一个里程碑或课程仓库后，把下面内容交给 GPT，并让它直接读取仓库：

```text
请对 backend-learning-lab 做一次只读、证据驱动的整仓审查，不要修改文件。

目标：这是面向初学者的 Python + Go 后端教程和练习仓库。
重点：正确性、教学递进、可运行性、安全、事务/并发/幂等、测试质量。
安全边界：这是公开仓库，禁止包含公司代码、内部地址、真实数据或凭据。

请先读取 README、LEARNING_ROADMAP、AGENTS.md，再按路线抽查课程、练习、测试、契约和综合项目。
请实际运行能够安全运行的 format/lint/test；没有依赖时说明限制，不要假装通过。

输出：
1. 总体结论；
2. P0-P3 问题（文件、触发场景、证据、最小修复、验证）；
3. 初学者可能被误导的内容；
4. 缺失的关键后端知识；
5. Python/Go/SQL/Redis/CI 的运行结果；
6. 是否发现敏感信息或疑似公司内容；
7. 建议的下一批最小修改。
```

## 判断 GPT 是否可信

- 是否引用了真实文件和代码，而不是泛泛而谈；
- 是否给出可触发场景；
- 是否区分“本地 fake 通过”和“生产验收”；
- 是否运行了测试并报告命令；
- 是否承认没有证据的部分；
- 修改后问题是否真的无法复现。
