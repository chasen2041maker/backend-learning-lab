# 如何用 ChatGPT 学后端、审代码、调 Bug，并把知识沉淀回仓库

这个仓库的主要学习方式不是“自己先按课程学完，再偶尔问 GPT”。

更适合的方式是：

```text
真实问题 / 当前代码
↓
和 ChatGPT 对话把心智模型讲通
↓
自己运行最小实验
↓
让 ChatGPT 审查理解或代码
↓
值得长期保留的内容整理进仓库
```

这份文件给出几种高价值用法。模板不是必须照抄，重点是告诉 AI：**你现在要解释、提示、审查、执行，还是沉淀知识。**

---

# 1. 直接学习一个概念

不需要先知道正确术语。

例如可以直接问：

```text
JWT 到底是什么？我知道登录后好像会拿到一个 token，但 Session、Cookie、Bearer、JWT 我混在一起了。

请不要只给定义。
先从“后端为什么需要它”讲，然后把这些词放在同一条请求链里。
每出现一个新词都解释它解决什么问题、和前一个词什么关系。
最后给我几个真实失败场景。
```

更通用模板：

```text
我正在理解：[概念/代码/系统行为]
我现在的理解是：[自己的话]
我卡在：[具体矛盾]

请：
1. 先指出我的理解哪些对、哪些不准确；
2. 从它解决的真实问题开始讲；
3. 画出输入 -> 状态变化 -> 输出；
4. 把容易混淆的相邻概念一起区分；
5. 给一个最小代码/请求示例；
6. 讲至少两个失败场景；
7. 最后给我几个关闭答案后应该能回答的问题。
```

---

# 2. 如果我只想听懂，不想马上写代码

可以明确：

```text
这一轮先不要给我任务和大段代码。
先把这个概念用后端数据流讲清楚，我会追问。
```

对话学习不是每次都必须立刻做 exercise。

但如果一个结论只有亲手制造失败才能真正理解，之后再补最小实验。

---

# 3. 学 Go 代码时：先解释控制流，不要逐行翻译

差的请求：

```text
逐行解释这段代码。
```

很容易变成语法翻译。

更好：

```text
我在学 Go 后端，这段代码我能看懂部分语法，但不知道整体在干什么。

先不要逐行解释。
请先告诉我：
1. 谁调用这个函数；
2. 输入从哪里来；
3. 状态在哪里改变；
4. 输出/错误到哪里；
5. goroutine/channel/context 在整条链里承担什么角色；
6. 然后再按关键代码块解释语法。
```

这样更容易建立后端心智模型。

---

# 4. 我想亲手写，不要直接生成完整答案

```text
我的目标：[一个很小的功能]
我已经会：[相关基础]
我想自己写。

请只给：
- 本次目标；
- 需要理解的 2～4 个知识点；
- 输入/输出约束；
- 验收标准；
- 应该写哪些测试；

先不要给完整实现。等我把代码发给你再 review。
```

这种方式适合真正建立编码能力。

如果你明确需要完整参考实现，可以直接说：

```text
这次给我完整可运行参考实现，然后再带我拆解。
```

不要把“永远不看完整答案”变成另一种形式主义。

---

# 5. 卡住时：不要让 AI 同时改五个地方

建议提供：

```text
目标：
预期：
实际：
完整错误/HTTP 响应：
最小相关代码：
我已经确认的事实：
我猜的原因：
```

然后要求：

```text
先不要直接修。
请判断我的根因假设能不能解释现有证据。
给我下一步最小实验；如果结果是 A/B，分别说明意味着什么。
```

这会训练 Debug，而不是训练复制 patch。

---

# 6. Debug 时让 AI 找“第一个错误状态”

复杂链路：

```text
Browser
-> Gateway
-> Service
-> Redis
-> PostgreSQL
```

用户最后只看到 500。

可以问：

```text
不要只解释最终 500。
帮我沿调用链定位“第一个和预期不同的状态”。
把已知事实和猜测分开；每个根因假设给一个最小验证实验。
```

这种问法比：

```text
为什么 500？
```

更容易得到可靠分析。

---

# 7. 读 AI/Codex 生成的大段代码

如果已经有完整代码，不要直接问“好不好”。

```text
这段代码由 AI 辅助生成，我要确认自己真正能掌控它。

请按顺序：
1. 画真实调用链；
2. 标出业务事实在哪里改变；
3. 标出 transaction/lock/timeout/retry 边界；
4. 标出身份和 tenant 从哪里来；
5. 推演在关键位置宕机会发生什么；
6. 找出哪些行为只有 fake test 证明、哪些有真实 integration 证据；
7. 给我 3 个故障实验。
```

如果无法解释这些，不要因为测试绿就认为代码已经“属于自己”。

---

# 8. 审查一个小功能

推荐要求“证据驱动”，防止 AI 为了显得有用硬凑问题。

```text
请审查这次修改。

需求：
不做什么：
契约变化：
数据/事务：
并发/重试：
安全边界：
验证命令：
已知限制：

只报告有证据的问题。
每个问题必须包含：
- 严重程度 P0/P1/P2/P3；
- 文件和具体位置；
- 可触发的输入/并发/故障场景；
- 实际后果；
- 为什么当前测试没有挡住；
- 最小修复；
- 修复后的验证。

如果没有确定问题，明确说没有，不要凑数量。
```

---

# 9. 整仓审查：不仅找 Bug，还审教学质量

这个仓库特殊之处是既有代码又有教程。

可以要求：

```text
请完整审查 backend-learning-lab。

它是一个“对话学习后沉淀知识 + 可运行实验”的后端学习仓库。

先读取 README、LEARNING_ROADMAP、AGENTS.md，再检查 lessons、notes、exercises、contracts、projects、progress。

重点检查：
1. 是否存在错误或可能误导初学者的结论；
2. 哪些 lesson 只有名词提纲，没有解释为什么/失败场景；
3. 学习顺序是否为了技术炫技而过早引入复杂组件；
4. exercise 是否真的证明对应不变量；
5. Python/Go/SQL/Redis 契约有没有漂移；
6. 综合项目是否过度设计；
7. 是否有重复、矛盾或无效文档；
8. 是否包含 Secret/真实数据/公司信息。

能安全执行的测试就实际执行；不能执行要说明限制。
不要为了“重构”改已经足够好的内容。
```

---

# 10. 让 ChatGPT 直接更新这个仓库

当 GitHub 连接有写权限时，可以直接说：

```text
这个知识点很有价值，整理进 backend-learning-lab。
先检查仓库已有内容，避免重复；如果旧文档不合理直接重构。
推送到远端后告诉我改了什么和 commit。
```

维护时合理顺序应该是：

```text
先读已有文件
↓
判断 journal / lesson / cheatsheet / exercise
↓
修正重复或旧错误
↓
提交
↓
重新读取/compare 验证远端真的更新
```

而不是每次都新建一个 MD。

---

# 11. 把今天讲通的内容写成 Learning Journal

可以说：

```text
把我们今天真正讲通的知识整理进 learning journal。
不要复制聊天原文。
保留：
- 我原来容易误解的点；
- 纠正后的心智模型；
- 一条完整数据流；
- 至少一个失败场景；
- 和已有后端知识的连接；
- 我以后应该能回答的问题。
如果这个主题已经有成熟 lesson，就只记录新的学习轨迹，不重复整章。
```

---

# 12. 什么时候应该重构 Lesson，而不是新增 Journal

如果发现已有 lesson：

```text
只有 20 行名词
```

但对话已经把主题讲出完整因果链，那么应该直接重构 lesson。

一个成熟 lesson 至少应该回答：

```text
为什么存在？
它在请求/数据链哪一层？
最小例子？
常见错误？
生产边界？
怎么亲手验证？
关闭文档后应该能讲什么？
```

这比不断增加零散 note 更重要。

---

# 13. 学系统设计时不要问“给我标准答案”

更好的问法：

```text
需求是：[需求]
假设规模：[大概数据]

不要直接给最终架构。
先带我做：
1. 功能/非功能需求；
2. 数量级估算；
3. source of truth；
4. 最小单服务方案；
5. 失败窗口；
6. 只有确认瓶颈后再一层层加 Redis/Queue/微服务。

每加一个组件都问我：它解决什么？不用会怎样？新增什么故障？
```

这样学到的是设计方法，而不是某一道面试题答案。

---

# 14. 学 Agent 时让 AI 同时做普通后端审查

```text
请不要只从 Prompt/模型角度审我的 Agent。
同时检查：
- authentication/tenant；
- retrieval ACL；
- tool authorization；
- side-effect idempotency；
- confirmation；
- timeout/deadline；
- token/step/cost budget；
- task persistence；
- audit；
- eval/observability。
```

这可以防止 Agent 代码“模型效果不错，但后端边界完全没有”。

---

# 15. 判断 AI 回答是否可信

优先看它有没有：

### 区分事实和猜测

差：

```text
肯定是连接池问题。
```

好：

```text
现有证据只说明请求在 DB 前停住；连接池是一个假设，可用 pool wait metric/stack 验证。
```

### 给可触发场景

差：

```text
可能有 race。
```

好：

```text
A/B 同时读 version=1，两个 UPDATE 都没带 version 条件，后写覆盖先写。
```

### 承认验证边界

```text
我只能静态审查，未实际运行 PostgreSQL integration test。
```

比假装“已验证生产正确”可靠。

### 不为了高级而加组件

如果 AI 一看到“高并发”就推荐：

```text
Kafka + K8s + microservices
```

但没做容量估算，应当继续追问理由。

---

# 16. 最值得保留的对话习惯

平时不用复杂 Prompt，也可以直接这样交流：

```text
我理解是不是……？
```

```text
为什么一定要这个？不用会怎样？
```

```text
给我一个会失败的例子。
```

```text
这跟我前面学的 transaction/JWT/context 有什么关系？
```

```text
先别给代码，把数据流讲清楚。
```

```text
这个我觉得很重要，写进仓库。
```

这些问题会持续把学习从“记名词”拉回“理解系统”。

---

# 17. 最后一个检查：不要让仓库替代学习本身

仓库应该帮助未来恢复知识，但不是目标。

错误：

```text
今天新增 5 个 MD
= 今天学了很多
```

正确：

```text
我以前解释不清 COMMIT 后响应丢失
今天我能自己画出来、写测试、解释幂等
= 今天建立了能力
```

文档只是把这项能力的关键心智模型保存下来。
